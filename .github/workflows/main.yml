name: first workflow

on: push

jobs:
  build:
      run-ons: ubuntu-latest
      steps:
      - uses: actions/checkout@v3

      - name: Running a sample github actions 
      - run: echo "This workflow will run as well"
